@page "/MyConverterMap"

<h3>Temperature Converter Using Map()</h3>

<div>
    <input type="number" @bind="_inputValue" placeholder="Enter value"/>
    <select @bind="_conversionType">
        <option value="FtoC">Fahrenheit to Celsius</option>
        <option value="CtoF">Celsius to Fahrenheit</option>
    </select>
    <button @onclick="ConvertTemperature">Convert</button>
</div>


<p>Result: @_result</p>

@code {
    private decimal _inputValue;
    private string _conversionType = "FtoC";
    private string _result = null!;

    private void ConvertTemperature()
    {
        _result = _conversionType switch
        {
            "FtoC" => TemperatureConversionServiceMap.FahrenheitToCelsius(_inputValue),
            "CtoF" => TemperatureConversionServiceMap.CelsiusToFahrenheit(_inputValue),
            _ => "Invalid conversion type"
        };
    }

}